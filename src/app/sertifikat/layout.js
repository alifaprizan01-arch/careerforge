export const metadata = {
  title: 'Sertifikat Saya | SiapKerja.id',
  description: 'Lihat dan unduh sertifikat pelatihan yang telah kamu selesaikan.',
};

export default function Layout({ children }) {
  return children;
}
