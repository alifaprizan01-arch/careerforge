export const metadata = {
  title: 'Booking Saya - Mentor | SiapKerja.id',
  description: 'Kelola jadwal booking sesi mentoring.',
};

export default function Layout({ children }) {
  return children;
}
