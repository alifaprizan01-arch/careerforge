export const metadata = {
  title: 'Pelatihan & Kursus | SiapKerja.id',
  description: 'Tingkatkan skill profesionalmu lewat pelatihan dari mentor ahli.',
};

export default function Layout({ children }) {
  return children;
}
