export const metadata = {
  title: 'Riwayat Lamaran | SiapKerja.id',
  description: 'Pantau status semua lamaran kerjamu di SiapKerja.id.',
};

export default function Layout({ children }) {
  return children;
}
