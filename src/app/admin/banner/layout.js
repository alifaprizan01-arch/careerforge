export const metadata = {
  title: 'Kelola Banner - Admin | SiapKerja.id',
  description: 'Kelola banner promosi platform SiapKerja.id.',
};

export default function Layout({ children }) {
  return children;
}
