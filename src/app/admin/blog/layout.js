export const metadata = {
  title: 'Kelola Blog - Admin | SiapKerja.id',
  description: 'Kelola konten blog dan artikel platform.',
};

export default function Layout({ children }) {
  return children;
}
