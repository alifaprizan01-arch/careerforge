export const metadata = {
  title: 'Kelola Lamaran - Admin | SiapKerja.id',
  description: 'Kelola data lamaran kerja pengguna.',
};

export default function Layout({ children }) {
  return children;
}
