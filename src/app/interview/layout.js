export const metadata = {
  title: 'Simulasi Interview AI | SiapKerja.id',
  description: 'Latih wawancara kerja dengan AI dan dapatkan feedback instan.',
};

export default function Layout({ children }) {
  return children;
}
